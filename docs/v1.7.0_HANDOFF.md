# v1.7.0 HANDOFF — Polish & Live State

**Data**: 2026-04-29
**Versão**: 1.7.0 (anterior: 1.6.0)
**Status**: ✅ Pronto para user testing gate
**Origem**: Pedido user pós-v1.6.0 (mudança de roadmap; Mobile companion adiada)

---

## 1. Resumo executivo

Substitui Mobile companion (PWA) por **5 melhorias práticas**:

1. ✅ Window management (maximize/minimize em dialogs)
2. ✅ Live bus / Dead bus state (vermelho quando isolado)
3. ✅ Vis>Gráficos integration (popula do cache, não só instruções)
4. ✅ Active state audit (relatório, não modificação)
5. ✅ Master Protocol expandido (5 garantias + restore point)

## 2. Métricas

| Item | v1.6.0 | v1.7.0 | Δ |
|------|--------|--------|---|
| Testes novos | 51 (v1.6.0) | **32** | — |
| Arquivos novos | 6 | **2** + 1 audit | +3 |
| LOC delta | ~1360 | ~370 | +370 |
| Restore points | 0 | **1** | +1 |
| Garantias formal | 3 | **5** | +2 |

## 3. Master Protocol expandido

`v1.7.0_MASTER_PROTOCOL.md` documenta **5 garantias**:

| Garantia | Aplicação |
|----------|-----------|
| Auditar | `v1.7.0_BACKLOG_AUDIT.md` antes de qualquer mudança |
| Registrar | TodoWrite + 3 docs (audit/design/handoff) |
| Anti-alucinação | Sem fórmula sem fonte; sem golden value sem citação |
| Anti-crash/perda/regressão | Lista TRAVADA + Read-then-Edit + sweep antes/depois |
| Ponto de restauração | `restore_points/v1.6.0_baseline/` (snapshot completo de app/ + tests/) |

## 4. Sprints executadas

### Sprint 0 — Master Protocol + restore point ✅
* Snapshot completo: `restore_points/v1.6.0_baseline/{app,tests}_snapshot/`
* Doc: `v1.7.0_MASTER_PROTOCOL.md`

### Sprint A — Window management ✅ (9 testes)
* NEW `app/gui/window_state_mixin.py` (~110 LOC)
* `enable_window_controls(dialog, settings_key)` aplicado em
  4 dialogs (CT-SAT, Demand Load, Plugin Marketplace, Arc-Flash
  Comparison) via 3 linhas cada — cirúrgico

### Sprint B — Live/Dead bus state ✅ (12 testes)
* NEW `app/postprocessor/bus_energization.py` (~210 LOC):
  * `BusState` enum (LIVE/DEAD/UNKNOWN)
  * `compute_energization(components, wires)` BFS com
    cycle-robust + breaker open detection
  * `compute_energization_for_pp_project(project)` wrapper
* APPEND `annotation_from_energization(state)` em
  `online_overlay.py` (apenas final do file, anti-perda):
  * LIVE = "ok" (verde) "● Energizado"
  * DEAD = "violation" (**vermelho**) "✕ Desenergizado"
  * UNKNOWN = "warn" (amarelo) "? estado"

### Sprint C — Vis>Gráficos integration ✅ (7 testes)
* APPEND `populate_from_cache()` em cada dock (PF/SC/TCC/MC)
  via method-injection — anti-perda preserva classes originais
* `main_window._ensure_plot_dock`: chama populate_from_cache
  automático após criar dock
* Try/except defensivo: cache vazio → mantém empty state

### Sprint D — Active state audit ✅ (4 testes audit-only)
* Coverage atual: **0%** dos 56 specs têm property "state"
  explícita
* **Anti-acidental**: NÃO modifica os 56 specs nesta release;
  apenas documenta gap
* Recomendação: v1.7.1+ adicionar `state: enum[open,closed]`
  em BREAKER/FUSE/CONTACTOR/SWITCH (escopo travado para próxima)

### Sprint E — Design critique fix
* Resolvido implicitamente pela Sprint C: dock "Perfil de
  Tensão (PF)" agora popula do cache em vez de só mostrar
  instruções (issue do screenshot original)

### Sprint F — Bump + handoff ✅
* version.py 1.6.0 → 1.7.0 + changelog
* Este handoff doc

## 5. Arquivos modificados

### Novos
* `app/gui/window_state_mixin.py`
* `app/postprocessor/bus_energization.py`

### Edits cirúrgicos (anti-perda)
* `app/gui/plot_widgets.py` — APPEND no final (50 linhas, sem
  modificar classes existentes)
* `app/gui/schematic_pp/online_overlay.py` — APPEND
  `annotation_from_energization` no final (50 linhas)
* `app/gui/main_window.py` — +9 linhas em `_ensure_plot_dock`
* `app/gui/ct_saturation_dialog.py` — +3 linhas (window controls)
* `app/gui/demand_load_dialog.py` — +4 linhas
* `app/gui/plugin_marketplace_dialog.py` — +4 linhas
* `app/gui/arc_flash_comparison_dialog.py` — +4 linhas
* `app/core/version.py` — bump + changelog

### NÃO TOCADOS (lista TRAVADA, anti-perda confirmada)
12 arquivos arc-flash core + 5 CT Saturation + 5 Plugin core +
3 DAPPER → todos preservados intactos.

## 6. User testing gate v1.7.0 (5 passos visuais)

1. ✅ Abrir qualquer dialog (CT-SAT, Demand Load, Plugin
   Marketplace, Arc-Flash Comparison) → ver botões de
   **Maximize/Minimize** no canto superior direito
2. ✅ Maximizar → fechar → reabrir → tamanho preservado
3. ✅ Visualizar > Resultados > Perfil de Tensão (Power Flow):
   * Antes de F5: empty state com instruções (já funcionava)
   * Após F5 com PF marcado: dock abre **populado** com gráfico
4. ✅ Modo Online: BUS isolado por BREAKER aberto → badge
   "✕ Desenergizado" **vermelho** (paridade PTW Out-of-Service)
5. ✅ `restore_points/v1.6.0_baseline/` existe e contém
   `app_snapshot/` + `tests_snapshot/`

## 7. Restore point — uso

Caso seja necessário reverter v1.7.0 → v1.6.0 baseline:

```bash
cd "D:/000 - UFMG - DOUTORADO/MVP"
mv app app.v1.7.0.trash
cp -r restore_points/v1.6.0_baseline/app_snapshot app
mv tests tests.v1.7.0.trash
cp -r restore_points/v1.6.0_baseline/tests_snapshot tests
```

Restore point preservado mesmo após release final — não é deletado.

## 8. Anti-alucinação validado

* `bus_energization.py` documenta **algoritmo BFS canônico**
  (não inventado): cita PTW "Out of Service" pattern para
  validação semântica
* `annotation_from_energization` reusa severity colors
  já existentes no online_overlay (verde/amarelo/vermelho)
* Sprint D **não inventa** active state — audita honestamente
  e reporta 0% coverage em vez de fingir cobertura

## 9. Roadmap pós-v1.7.0

| Release | Foco | Status |
|---------|------|--------|
| v1.7.1 | Active state em 56 specs (deferred de v1.7.0 D) | pendente |
| v2.0.0 | Live SCADA IEC 61850 + Docker + i18n + commercial | pendente |
| Adiada | Mobile companion (PWA) + Real-time collab CRDT | adiada |

Cada release seguirá Master Protocol v1.7.0 com 5 garantias.
