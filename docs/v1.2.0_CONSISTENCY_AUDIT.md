# v1.2.0 Consistency Audit — Pré-Fase γ

**Data**: 2026-04-28
**Escopo**: auditoria sistemática da base v1.2.0 (Fases α + β) antes de iniciar Fase γ.
**Princípio**: anti-alucinação — toda métrica vem de inspeção AST automatizada, não de memória.

---

## Sumário Executivo

| Dimensão | Antes do audit | Após fixes |
|---|---|---|
| Segments com cobertura normativa em docstring | 11/16 | **16/16** ✅ |
| Devices com cobertura normativa em docstring | 1/3 | **3/3** ✅ |
| Print() em código de produção | 0 | 0 ✅ |
| Imports proibidos (scipy/numpy/matplotlib/pandas) | 0 | 0 ✅ |
| Field `segment_id` + `enabled` em todo TCCSegment* | 16/16 | **16/16** ✅ |
| Method `time_at_current` + `points` em todo TCCSegment* | 16/16 | **16/16** ✅ |
| Naming consistency (pickup_A vs pickup_amps) | OK (1 exceção justificada) | OK ✅ |
| Total tests v1.2.0 | 157 | **157** (mantido) |
| Regression v1.1.0 quebrada | 0 | **0** ✅ |

**Score**: 100/100 após 6 fixes aplicados.

---

## Metodologia

Auditoria 100% **automatizada via Python AST**:

```python
import ast, re

with open('app/postprocessor/tcc_segments.py') as f:
    tree = ast.parse(f.read())

# 1. Para cada classe TCCSegment*, verifica:
#    - tem field 'segment_id'
#    - tem field 'enabled'
#    - tem método 'time_at_current'
#    - tem método 'points'
#    - docstring cita norma IEC/IEEE/ANSI/NBR/NEMA/NFPA OU
#      manual PTW (Reference-CAPTOR.pdf)

# 2. Tests v1.2.0/* — pattern naming + classes + counts
```

Sem julgamento humano, sem alucinação — só fatos do AST.

---

## 1. Audit dos 16 TCCSegment*

### 1.1 Atributos comuns

| Class | # fields | required (segment_id+enabled) | first 5 fields |
|---|---|---|---|
| TCCSegmentLT | 5 | OK | `segment_id, pickup_A, curve_type, tms, enabled` |
| TCCSegmentST | 4 | OK | `segment_id, pickup_A, delay_s, enabled` |
| TCCSegmentINST | 4 | OK | `segment_id, pickup_A, inherent_delay_s, enabled` |
| TCCSegmentDefiniteTime | 4 | OK | `segment_id, pickup_A, delay_s, enabled` |
| TCCSegmentAdjustableTolerance | 6 | OK | `segment_id, pickup_A_nominal, curve_type, tms, tolerance_pct` |
| TCCSegmentI2T | 5 | OK | `segment_id, pickup_A, K_I2T_kA2_s, time_floor_s, enabled` |
| TCCSegmentTimeCurrentPoints | 3 | OK | `segment_id, point_pairs, enabled` |
| TCCSegmentFixTimeHorizontal | 5 | OK | `segment_id, time_s, i_min_A, i_max_A, enabled` |
| TCCSegmentAnalyticAD | 8 | OK | `segment_id, pickup_A, AD, N, C` |
| TCCSegmentAnalyticSM | 9 | OK | `segment_id, pickup_A, SM, A, B` |
| TCCSegmentOpening | 4 | OK | `segment_id, pickup_A, opening_time_s, enabled` |
| TCCSegmentClearing | 4 | OK | `segment_id, pickup_A, clearing_time_s, enabled` |
| TCCSegmentOpenClearCurve | 4 | OK | `segment_id, base_segment, arc_extinguishing_s, enabled` |
| TCCSegmentOpenClearBand | 4 | OK | `segment_id, opening_segment, clearing_segment, enabled` |
| TCCSegmentSlopeT | 5 | OK | `segment_id, pickup_A, K, slope, enabled` |
| TCCSegmentDelayBand | 4 | OK | `segment_id, min_segment, max_segment, enabled` |

**Inconsistência justificada**: `TCCSegmentAdjustableTolerance` usa `pickup_A_nominal` (não `pickup_A`) porque tem 3 valores conceituais — nominal, min (=nom×(1-tol)), max (=nom×(1+tol)). Documentado no docstring.

**Conclusão**: 16/16 conformes ao Protocol `TCCSegment`.

### 1.2 Métodos required

100% das classes têm `time_at_current(I)` e `points(i_min, i_max, n)`.

Classes com métodos extras (helpers para uso especializado):
- `TCCSegmentAdjustableTolerance` (8): + `time_envelope_min/max`, `envelope_min_points/max_points`, `_pickup_min/max`
- `TCCSegmentTimeCurrentPoints` (5): + `_i_min/max`, `__post_init__` validation
- `TCCSegmentOpenClearBand` (6): + `time_opening/clearing`, `opening_points/clearing_points`
- `TCCSegmentDelayBand` (6): + `time_min/max`, `min_points/max_points`

### 1.3 Cobertura normativa em docstrings

**Antes do audit**: 4 classes sem citação detectada (FixTimeHorizontal, OpenClearCurve, OpenClearBand, DelayBand).

**Fix aplicado**: adicionada seção "Cobertura" com normas explícitas:

| Class | Normas adicionadas |
|---|---|
| TCCSegmentFixTimeHorizontal | IEC 60255-151 §6.3.3, PTW Reference-CAPTOR, IEEE C37.04 |
| TCCSegmentOpenClearCurve | IEEE C37.04, IEC 62271-100, IEEE 242 §15.10, PTW |
| TCCSegmentOpenClearBand | IEC 60269-1:2014, IEEE C37.04, IEEE 242 §15.5.4, PTW |
| TCCSegmentDelayBand | IEC 60255-151 §6.3, IEEE C37.112-2018, PTW |

**Após fix**: 16/16 com cobertura ✅.

---

## 2. Audit do tcc_devices.py

### 2.1 Classes auditadas

| Class | docstring norma | Status |
|---|---|---|
| TCCDevice | (era MISSING) → IEC 60255-151, IEEE C37.2/04, IEEE 242, PTW | ✅ FIXED |
| MultiFunctionRelay | IEC 60255, IEEE C37 | ✅ OK pre-existente |
| DeviceCoordinationCheck | (era MISSING) → IEEE Std 242-2001 §15.10.1, PTW | ✅ FIXED |

**Após fix**: 3/3 com cobertura ✅.

### 2.2 Estrutura de imports

```python
# tcc_segments.py imports:
from app.postprocessor.tcc_curves import CurveType, operating_time_s

# tcc_devices.py imports:
from app.postprocessor.tcc_segments import TCCSegment, composite_time_at_current
# Lazy import (dentro de função) para evitar circular:
#   from app.postprocessor.tcc_curves import COORDINATION_MARGIN_S
```

Dependência limpa: `tcc_curves` (legacy) ← `tcc_segments` (α) ← `tcc_devices` (β).
Sem circular dependency. Reuso de constantes do legacy quando aplicável.

---

## 3. Audit dos tests v1.2.0

| Arquivo | Test classes | Tests | Norma refs no código |
|---|---|---|---|
| `test_pp_v1_2_0_tcc_segments_alpha1.py` | 6 | 21 | 2 |
| `test_pp_v1_2_0_tcc_segments_alpha2.py` | 5 | 23 | 2 |
| `test_pp_v1_2_0_tcc_segments_alpha3.py` | 3 | 14 | **12** (validação canônica IEC/ANSI) |
| `test_pp_v1_2_0_tcc_segments_alpha4.py` | 5 | 19 | 3 |
| `test_pp_v1_2_0_tcc_segments_alpha5.py` | 3 | 15 | 0 |
| `test_pp_v1_2_0_tcc_devices_beta1.py` | 7 | 25 | 0 |
| `test_pp_v1_2_0_tcc_devices_beta2.py` | 5 | 18 | 0 |
| `test_pp_v1_2_0_tcc_devices_beta3.py` | 5 | 14 | 0 |
| **TOTAL** | **39** | **157** | **19** |

**Observação positiva**: α.3 (analytic formulas) tem **12 referências normativas** porque inclui os 3 testes canônicos validando contra `operating_time_s` legacy de IEEE C37.112-2018 — anti-alucinação por equivalência matemática.

**Convenção naming**: 100% dos arquivos seguem `test_pp_v1_2_0_<modulo>_<sub-sprint>.py`.
Classes seguem `TestX` (PEP 8 + pytest convention).

---

## 4. Audit de qualidade do código

| Critério | Resultado |
|---|---|
| Print() em produção (anti-pattern) | 0 |
| Imports proibidos (scipy, numpy, matplotlib, pandas) | 0 |
| Type hints presentes | 100% (frozen dataclasses + Protocol) |
| Docstrings em todas as classes públicas | 100% |
| Citação normativa em docstring | 100% (após fixes) |
| Validação `__post_init__` em segments com pré-condições | 1 (TimeCurrentPoints — único com regras complexas) |

---

## 5. Issues encontrados e resolvidos

### Issue 1 — 4 docstrings sem cobertura normativa explícita
- **Detecção**: regex AST contra padrões IEC/IEEE/ANSI/NBR/NEMA/NFPA + PTW manual
- **Fix**: adicionada seção "Cobertura" em FixTimeHorizontal, OpenClearCurve, OpenClearBand, DelayBand
- **Validação**: re-run audit confirma 16/16 OK

### Issue 2 — 2 docstrings em tcc_devices.py sem cobertura
- **Detecção**: idem
- **Fix**: adicionada seção "Cobertura normativa" em TCCDevice e DeviceCoordinationCheck
- **Validação**: 3/3 OK

### Issue 3 — Inconsistência nomeada justificada
- **Detecção**: `pickup_A_nominal` em AdjustableTolerance vs `pickup_A` nos outros
- **Análise**: justificada — AdjustableTolerance tem 3 valores conceituais (nominal, min, max). Não fix.
- **Documentação**: já presente no docstring da classe.

---

## 6. Conformidade com princípios v1.2.0

| Princípio | Status |
|---|---|
| Não modificar tcc_curves.py legacy | ✅ Verificado por inspeção (último mtime do legacy é da v1.1.0) |
| TDD por sub-sprint | ✅ Cada α.X / β.X tem arquivo de teste próprio |
| Validação anti-alucinação por canonical tests | ✅ α.3 tem 3 canonical vs operating_time_s |
| Documentação inline com normas | ✅ 100% após fixes |
| Sub-sprints reverterveis | ✅ Cada arquivo é independente |
| Log incremental | ✅ `v1.2.0_PROGRESS_LOG.md` atualizado |

---

## 7. Métricas finais v1.2.0 (Fases α + β)

| Métrica | Valor |
|---|---|
| Segment types implementados | 16 (paridade CAPTOR) |
| Multi-function devices | ✅ TCCDevice + MultiFunctionRelay |
| Coordination check device-vs-device | ✅ DeviceCoordinationCheck + multi_point |
| LOC `tcc_segments.py` | 1382 |
| LOC `tcc_devices.py` | 678 (após fix de docstrings) |
| LOC tests v1.2.0 | 2253 |
| Total tests v1.2.0 novos | 157 |
| Sweep ampla (legacy + α + β + ptw_symbols + online_overlay) | 248/248 verde |
| Cobertura normativa em docstrings | 16/16 segments + 3/3 devices |

---

## 8. Pronto para Fase γ?

**Resposta**: ✅ SIM.

Pré-requisitos checados:
- [x] Fase α completa e validada
- [x] Fase β completa e validada
- [x] Audit de consistência aprovado (100/100 após fixes)
- [x] Sem regressão na sweep
- [x] Documentação inline e externa atualizadas
- [x] Princípios anti-colapso/anti-alucinação respeitados

**Próximo passo**: criar `docs/v1.2.0_GAMMA_DESIGN.md` com plano detalhado de:
- γ.1 — Cable damage curve (NBR 5410 §6.5.4)
- γ.2 — Transformer through-fault (IEEE C57.109)
- γ.3 — Motor thermal limit (IEC 60079-7 / NEMA MG-1)

Cada um seguindo: design doc → TDD → validação → log update.
