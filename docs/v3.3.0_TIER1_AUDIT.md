# v3.3.0 Tier 1 Algorithmic Audit — Olivas v3.2.0

**Data**: 2026-05-01
**Trigger**: pre-v3.3.0 deep audit (lição v3.2.0 — 7ª garantia)
**Escopo**: 4 módulos algorítmicos extendidos em v3.3.0

---

## A. `power_flow.py` (710 LOC)

### A.1 Estado atual
- `BusType` enum: SLACK / PV / PQ
- `PfBus`, `PfBranch`, `LineFlow`, `PowerFlowSolution`, `PowerFlowSystem` (builder)
- `solve_power_flow()` — Newton-Raphson polar com Jacobiano completo
- Convergence: ‖ΔP; ΔQ‖∞ < tol (default 1e-6)
- Stevenson §9 Example 9.5 golden test (`tests/test_pp_power_flow.py:250-283`)

### A.2 Gaps vs PTW Tutorial §Part 2
| Gap | Status |
|-----|--------|
| NR full Jacobian | ✅ |
| PV→PQ Q-limit switching | ❌ |
| Fast-Decoupled (FDLF) | ❌ |
| Gauss-Seidel | ❌ |
| DC Load Flow | ❌ |
| Stevenson 3-bus golden | ✅ |
| **IEEE 14-bus golden** | ❌ ausente |
| Phase-shifting transformer | ❌ |
| Multiple slacks | ❌ |
| Tap-ratio off-nominal | ✅ (parcial) |

### A.3 Trigger GUI
- **`PowerFlowDialog`** (`analysis_dialogs.py:254-322`) — **HARD-CODED a 2-bus** (1 SLACK + 1 PQ + 1 branch)
- ⚠️ Não lê topologia do `.sch` real

---

## B. `power_flow_unbalanced.py` (261 LOC)

### B.1 Estado atual
- `BusUnbalancedVoltage`, `UnbalancedPFResult`
- `phase_to_sequence()` / `sequence_to_phase()` — Fortescue completo ✅
- `analyze_unbalanced_pf()` — **PLACEHOLDER analítico**, NÃO solver real

### B.2 Gaps críticos
- ❌ **NÃO É PF unbalanced real** — apenas aplica Fortescue inverso a V_neg=0.5% típico
- ❌ Sem matriz Y_012, sem N-R 3-φ
- ❌ Sem open-phase scenarios (Tutorial §Part 9 p.241)
- ❌ Sem per-phase load
- ❌ Sem datablocks Phase A/B/C

### B.3 Trigger GUI
- ❌ **VIOLAÇÃO 7ª GARANTIA** — sem dialog dedicado, sem trigger no menu Análise
- Buscado em `app/gui/*.py` por "unbalanced" → 0 hits

---

## C. `equipment_eval.py` (360 LOC) + `equipment_eval_rules.py`

### C.1 Estado atual
- `EquipmentInstance`, `EvaluationReport` (9 critérios PTW)
- 9 critérios:
  1. EQ-VOLTAGE-RATING, 2. EQ-INTERRUPT-DUTY, 3. EQ-ASYM-DUTY,
  4. EQ-LOAD-FLOW-CURRENT, 5. EQ-DESIGN-LOAD, 6. EQ-GENERATOR-CAPACITY,
  7. EQ-BUS-VOLTAGE-DROP, 8. EQ-BRANCH-VOLTAGE-DROP, 9. EQ-DEVICE-VOLTAGE-DROP
- Severity: <70% PASS / 70-100% WARN / >100% FAIL

### C.2 Gaps vs PTW Tutorial §Part 4
| Gap | Status |
|-----|--------|
| 9 critérios PTW | ✅ |
| Branch vs Bus split | ✅ (mas TX faltando em branch V-drop) |
| ANSI vs IEC selector | ❌ |
| Filter por tipo (UI) | ❌ |
| User-defined limits (Project>Options) | ⚠️ Parcial via `context` |
| Auto-extract de PpProject | ❌ Dialog usa demo |

### C.3 Trigger GUI
- **`EquipmentEvalDialog`** (`equipment_eval_dialog.py`) — Ctrl+E ✅
- ⚠️ Carrega 7 equipamentos demo, não lê do projeto

---

## D. `iec60909.py` (557 LOC) + sub-módulos

### D.1 Estado atual
- `voltage_factor_c()`, `kappa_factor()`, `network_feeder_impedance_ohm()`,
  `transformer_impedance_ohm()`, `synchronous_machine_impedance_ohm()`
- `initial_symmetric_sc_current_kA()`, `peak_sc_current_kA()`, `dc_component_kA()`
- `calculate_short_circuit()` wrapper
- Sub-módulos: `iec60909_seq.py` (sequence), `iec60909_decay.py` (μ·q), `iec60909_kappa.py` (Methods A/B/C)

### D.2 Gaps vs IEC 60909-0:2016
| Gap | Status |
|-----|--------|
| Voltage factor c Tab 1 | ✅ |
| Methods A (κ) | ✅ integrado |
| Methods B/C (κ) | ⚠️ Existem em `iec60909_kappa.py`, **não integrados** ao `calculate_short_circuit` |
| Decay μ·q (Ib, Ik) | ⚠️ Existe em `iec60909_decay.py`, **não integrado** (`Ib = Ik = Ik''` hard-coded) |
| **KT (transformer §6.3.3, Tab 3)** | ❌ |
| **KG / KSO (Tab 2)** | ❌ |
| Sequence faults LL/LG/LLG | ✅ |

### D.3 Trigger GUI
- **`ShortCircuitDialog`** (F5) ✅
- ⚠️ **Dropdown κ Method A/B/C** existe na UI mas é **inerte** — `kappa_method` coletado mas descartado em `run_short_circuit_analysis`

---

## E. Plano consolidado v3.3.0 (5 sub-sprints)

| Sprint | Foco | Esforço | Trigger 7ª garantia |
|--------|------|---------|----------------------|
| 1 | Unbalanced PF — Dialog GUI (FECHA violação 7ª) + open-phase | M | ✅ NEW UnbalancedPowerFlowDialog |
| 2 | IEC 60909 — wire kappa_method dropdown + integrar decay μ·q | M | ✅ via ShortCircuitDialog |
| 3 | LF NR — IEEE 14-bus golden test + Q-limits | M | ✅ via PowerFlowDialog |
| 4 | EquipmentEval — auto-extract de PpProject + filter UI | S | ✅ via EquipmentEvalDialog |
| 5 | KT/KG/KSO factors + final 7ª garantia audit | S | (cross) |

---

## F. Achados críticos (resumo executivo)

1. ❌ **`power_flow_unbalanced.py` viola 7ª garantia** — sem dialog GUI
2. ⚠️ **`ShortCircuitDialog` tem UI inerte** — dropdown κ não conectado
3. ⚠️ **`PowerFlowDialog` é 2-bus only** — não lê topologia real
4. ⚠️ **`EquipmentEvalDialog` usa só demo** — sem integração PpProject
5. ❌ **Sem IEEE 14-bus golden** — único multi-bus é Stevenson 3
6. ⚠️ **decay μ·q existe mas inerte** — `Ib = Ik = Ik''` hard-coded
7. ❌ **Sem KT/KG/KSO** factors (IEC §6.3.3, §6.6.2)
8. ⚠️ **Duplicação** `kappa_factor` vs `kappa_method_A`

**Estratégia v3.3.0**: priorizar fechamento da violação 7ª garantia (Sprint 1 — Unbalanced PF Dialog) e wiring de UI inertes (Sprint 2 — kappa_method) antes de features novas.
